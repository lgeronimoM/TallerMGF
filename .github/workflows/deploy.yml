name: Build & Deploy TallerMGF (Docker Hub)

on:
  push:
    branches: [ master ]

jobs:
  deploy:
    runs-on: self-hosted
    env:
      DOCKERHUB_REPO: lgeronimom/tallermgf
      NAMESPACE: tallermgf
    
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4
      
      - name: 🔍 Herramientas
        run: |
          kubectl version --client=true
          kubectl cluster-info
      
      - name: 🔐 Login Docker Hub
        env:
          DH_USER: ${{ secrets.DOCKERHUB_USERNAME }}
          DH_PASS: ${{ secrets.DOCKERHUB_TOKEN }}
        run: |
          echo "$DH_PASS" | docker login -u "$DH_USER" --password-stdin
      
      - name: 🏗️ Build & Push (tags SHA y latest)
        env:
          IMAGE_SHA: ${{ env.DOCKERHUB_REPO }}:${{ github.sha }}
          IMAGE_LATEST: ${{ env.DOCKERHUB_REPO }}:latest
        run: |
          docker build -t "$IMAGE_SHA" .
          docker tag "$IMAGE_SHA" "$IMAGE_LATEST"
          docker push "$IMAGE_SHA"
          docker push "$IMAGE_LATEST"
          echo "IMAGE_SHA=$IMAGE_SHA" >> $GITHUB_ENV
      
      - name: 🚀 Apply manifests
        run: |
          kubectl apply -f k8s/namespace.yaml
          kubectl apply -f k8s/configmap.yaml
          kubectl apply -f k8s/deployment.yaml
          kubectl apply -f k8s/service.yaml
          kubectl apply -f k8s/ingress.yaml
      
      - name: 🔄 Actualizar imagen a la del commit (SHA)
        run: |
          kubectl -n $NAMESPACE set image deployment/tallermgf tallermgf=${IMAGE_SHA} --record
      
      - name: ⏳ Esperar rollout
        run: kubectl -n $NAMESPACE rollout status deploy/tallermgf --timeout=300s
      
      - name: ✅ Verificación
        run: |
          kubectl -n $NAMESPACE get deploy,pods,svc,ingress -o wide
          kubectl -n $NAMESPACE get events --sort-by=.lastTimestamp | tail -50
      
      - name: 🧰 Diagnóstico si falla
        if: failure()
        run: |
          kubectl -n $NAMESPACE describe deployment/tallermgf || true
          kubectl -n $NAMESPACE describe pods || true
          kubectl -n $NAMESPACE get events --sort-by='.lastTimestamp' | tail -150 || true

      # GitHub Metrics
      - name: 📊 Reportar estado del deploy
        if: always()
        env:
          INFLUX_URL: ${{ secrets.INFLUX_URL }}
          INFLUX_ORG: ${{ secrets.INFLUX_ORG }}
          INFLUX_BUCKET: ${{ secrets.INFLUX_BUCKET }}
          INFLUX_TOKEN: ${{ secrets.INFLUX_TOKEN }}
          STATUS: ${{ failure() && 'failure' || cancelled() && 'cancelled' || 'success' }}
        run: |
          # Escapar caracteres especiales para InfluxDB
          escape() { echo "$1" | sed 's/[ ,=]/\\&/g'; }
          
          USER=$(escape "${{ github.actor }}")
          REPO=$(escape "${{ github.repository }}")
          BRANCH=$(escape "${{ github.ref_name }}")
          STATUS_ESC=$(escape "$STATUS")
          
          # Usar timestamp del commit o actual
          TIMESTAMP=${{ github.event.head_commit.timestamp && 'date -u -d "${{ github.event.head_commit.timestamp }}" +%s%N' || 'date -u +%s%N' }}
          TS=$(eval $TIMESTAMP)
          
          # Enviar métrica a InfluxDB
          LINE="github_deploys,user=${USER},repo=${REPO},branch=${BRANCH},status=${STATUS_ESC} value=1 ${TS}"
          
          curl -sfS -X POST "$INFLUX_URL/api/v2/write?org=$INFLUX_ORG&bucket=$INFLUX_BUCKET" \
            -H "Authorization: Token $INFLUX_TOKEN" \
            -H "Content-Type: text/plain; charset=utf-8" \
            -d "$LINE"
